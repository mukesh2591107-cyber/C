#include <stdio.h>
//enum programs 
1)enum Days {MON, TUE, WED, THU, FRI, SAT, SUN};
int main() 
{
    enum Days w;
    w=WED;
    printf("w = %d", w); 
    return 0;
}
2)enum Colors {RED, GREEN, BLUE};
int main() 
{
    enum colours;
    printf("RED=%d\n", RED);
    printf("GREEN=%d\n", GREEN);
    printf("BLUE=%d\n", BLUE);
    return 0; 
}
3)enum Colors {red, green, blue};
int main() 
{
    int n ;
    scanf("%d", n);
    switch(n) 
    {
       case red:
       printf("red");
        break;
       case green:
       printf("green");
        break;
       case blue: 
       printf("blue");
        break;
        default: printf("colours");
    }
    return 0;
}
//typedef programs
1)int main()
{
    typedef unsigned int unit;
    unit i,j;
    i=10;
    j=15;
    printf("i= %d\n",i);
    printf("j= %d",j);
    return 0;
}
2)typedef float unit;
int main()
{
    unit x,y;
    x = 6;
    y = 4;
    printf("Average = %f", (x + y) / 2);
    return 0;
}
//macro programs
1)#define square(n) ((n) * (n))
int main() {
    int n = 5;
    printf("square = %d", square(n));
    return 0;
}
2)#define max(a, b) ((a) > (b)? (a) : (b))
int main()
{
    int a=2;
    int b=1;
    printf("max = %d", max(2, 1));
    return 0;
}
