#include <stdio.h>
int main()
{
    FILE*p;
    p=fopen ("mukesh.txt","w");
    printf ("success");
    fclose (p);
    return 0;
}

 #include <stdio.h>

int main()
{
    FILE *fp;
    char ch;
    int n=0, words=0, lines=0;
    fp = fopen("text.txt","r");
    while((ch=fgetc(fp))!=EOF)
    {
        n++;
        if(ch==' '||ch=='\n'||ch=='\t')
            words++;
        if(ch=='\n')
            lines++;
    }
    printf("Characters = %d\n", n);
    printf("Words = %d\n", words+1);
    printf("Lines = %d\n", lines+1);
    fclose(fp);
    return 0;
}  

